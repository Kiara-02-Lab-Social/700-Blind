# CatalogApp

Create an iOS app target in Xcode and import `SevenHundredBlind` to exercise each pattern on a real device or simulator.

The public catalog should always distinguish between:

- recorded real iOS behavior,
- web analogues used for explanation,
- accessibility claims that have actually been tested.
